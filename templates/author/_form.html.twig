{{ form_start(form) }}
<div class="mb-3">
	{{ form_label(form.name, 'My label for name',{'label_attr': {'class': 'form-label'}}) }}
	{{ form_widget(form.name, { 'attr': {'class': 'form-input', 'placeholder':"Name", 'aria-label':"Name"}}) }}
</div>
<div class="mb-3">
	<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
